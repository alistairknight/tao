# Pokemon Pokedex Example

Sample pokedex using Python, Django and React. Data collected from https://pokemondb.net/ and loaded into a local SQLite DB.

## Getting Started

Create virtual environment, install dependencies.

### Prerequisites

Use pip to install dependencies i.e. pip -r requirements.txt

Once installed, run "python manage.py runserver" to start the app, then navigate a web browser to http://127.0.0.1:8000
